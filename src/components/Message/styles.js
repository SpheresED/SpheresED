import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    margin: 5,
    padding: 10,
    borderRadius: 10,
    maxWidth: "80%",
  },
  message: {},
  time: {
    color: "gray",
    alignSelf: "flex-end",
  },
});

export default styles;
